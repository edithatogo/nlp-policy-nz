# Specification

Generate version-aligned `CITATION.cff` and `.zenodo.json` release assets,
publish the assets with GitHub releases, and maintain a machine-readable DOI
manifest. Version and concept DOIs remain null and the publication status
remains `unverified` until a live Zenodo record and record URL are verified.
