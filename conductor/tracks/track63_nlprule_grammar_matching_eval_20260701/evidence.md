# Track 63 Evidence

Track 63 evaluates nlprule-style grammar and rule matching with a deterministic
spaCy-backed comparator. The evaluation keeps Te Reo Māori tokens intact,
maps matches into the existing extraction and quality schemas, and records a
clear dependency decision instead of introducing a required nlprule runtime.

